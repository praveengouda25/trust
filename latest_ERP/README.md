# SVRST ERP

Hostel, student, and welfare operations for SVRST Trust.

In development it runs on port 5174. In production it is served at `/erp` on the same Vercel deployment as the public website.

```sh
npm install
npm run dev
```

Open http://localhost:5174/auth.
